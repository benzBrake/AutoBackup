# AutoBackup

Typecho 数据库自动备份插件，支持将同一份备份发送至邮箱、上传至 WebDAV，或同时投递到两个目标。

## 使用

1. 启用插件并进入设置页，选择需要备份的数据表。
2. 在“发送邮件”和“WebDAV”标签页中配置并启用至少一个备份目标。
3. 将设置页显示的备份调用地址加入计划任务。

WebDAV 使用 HTTP Basic Auth，通过 `MKCOL` 自动创建远端目录，并通过 `PUT` 上传备份文件。PHP 需要启用 cURL 扩展。

如果对你有用，请 Star。
